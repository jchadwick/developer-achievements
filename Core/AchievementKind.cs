namespace ChadwickSoftware.DeveloperAchievements
{
    public enum AchievementKind
    {
        Accumulator,
        Streak,
        Medal,
    }

    public enum AchievementDisposition
    {
        Neutral = 0,
        Positive,
        Negative,
    }
}